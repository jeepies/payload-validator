import { beforeEach, describe, expect, test } from "@jest/globals";

import validator from '../src/payload-validator';

describe('🧪 Validator Tests', () => {
    test("should create a new schema", () => {
        const schema = new validator.obj({
            
        });
    })
});